import { loginAdmin } from "./functions/loginAdminFunction.js";
window.onload = function () {
  // Checks if already logged in
  // loggedInChecker();

  // The function for logging in as an admin
  loginAdmin();
};
